# This is a config for PPIO Sandbox template.
# You can use template ID (nz0r87p15riw7uq0py2g) or template name (r2e-coveragepy) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("r2e-coveragepy") # Sync sandbox
# sandbox = await AsyncSandbox.create("r2e-coveragepy") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('r2e-coveragepy')

dockerfile = "ppio.Dockerfile"
template_name = "r2e-coveragepy"
template_id = "nz0r87p15riw7uq0py2g"
