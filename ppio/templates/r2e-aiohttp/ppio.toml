# This is a config for PPIO Sandbox template.
# You can use template ID (2zegsf0jse5jwl69rcg4) or template name (r2e-aiohttp-v2) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("r2e-aiohttp-v2") # Sync sandbox
# sandbox = await AsyncSandbox.create("r2e-aiohttp-v2") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('r2e-aiohttp-v2')

dockerfile = "ppio.Dockerfile"
template_name = "r2e-aiohttp-v2"
template_id = "2zegsf0jse5jwl69rcg4"
