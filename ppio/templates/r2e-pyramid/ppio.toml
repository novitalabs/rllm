# This is a config for PPIO Sandbox template.
# You can use template ID (jnf8bjsqbgp5btvpkiq7) or template name (r2e-pyramid) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("r2e-pyramid") # Sync sandbox
# sandbox = await AsyncSandbox.create("r2e-pyramid") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('r2e-pyramid')

dockerfile = "ppio.Dockerfile"
template_name = "r2e-pyramid"
template_id = "jnf8bjsqbgp5btvpkiq7"
