# This is a config for PPIO Sandbox template.
# You can use template ID (ielvh8pvyrtpbfpomofl) or template name (swebench-django-django) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("swebench-django-django") # Sync sandbox
# sandbox = await AsyncSandbox.create("swebench-django-django") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('swebench-django-django')

dockerfile = "ppio.Dockerfile"
template_name = "swebench-django-django"
template_id = "ielvh8pvyrtpbfpomofl"
