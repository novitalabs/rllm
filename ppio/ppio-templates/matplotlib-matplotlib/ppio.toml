# This is a config for PPIO Sandbox template.
# You can use template ID (sefq769czundzf189f66) or template name (swebench-matplotlib-matplotlib) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("swebench-matplotlib-matplotlib") # Sync sandbox
# sandbox = await AsyncSandbox.create("swebench-matplotlib-matplotlib") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('swebench-matplotlib-matplotlib')

dockerfile = "ppio.Dockerfile"
template_name = "swebench-matplotlib-matplotlib"
template_id = "sefq769czundzf189f66"
