# This is a config for PPIO Sandbox template.
# You can use template ID (pre8akkasgo8bt49n38e) or template name (swebench-pallets-flask) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("swebench-pallets-flask") # Sync sandbox
# sandbox = await AsyncSandbox.create("swebench-pallets-flask") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('swebench-pallets-flask')

dockerfile = "ppio.Dockerfile"
template_name = "swebench-pallets-flask"
template_id = "pre8akkasgo8bt49n38e"
