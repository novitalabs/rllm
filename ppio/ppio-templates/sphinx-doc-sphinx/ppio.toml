# This is a config for PPIO Sandbox template.
# You can use template ID (g969p0jo8p4auyfqoal1) or template name (swebench-sphinx-doc-sphinx) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("swebench-sphinx-doc-sphinx") # Sync sandbox
# sandbox = await AsyncSandbox.create("swebench-sphinx-doc-sphinx") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('swebench-sphinx-doc-sphinx')

dockerfile = "ppio.Dockerfile"
template_name = "swebench-sphinx-doc-sphinx"
template_id = "g969p0jo8p4auyfqoal1"
