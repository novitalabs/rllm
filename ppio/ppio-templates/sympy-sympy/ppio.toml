# This is a config for PPIO Sandbox template.
# You can use template ID (hv9wnalh0da3fl6wll75) or template name (swebench-sympy-sympy) to create a sandbox:

# Python SDK
# from ppio_sandbox.core import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("swebench-sympy-sympy") # Sync sandbox
# sandbox = await AsyncSandbox.create("swebench-sympy-sympy") # Async sandbox

# JS SDK
# import { Sandbox } from 'ppio-sandbox'
# const sandbox = await Sandbox.create('swebench-sympy-sympy')

dockerfile = "ppio.Dockerfile"
template_name = "swebench-sympy-sympy"
template_id = "hv9wnalh0da3fl6wll75"
